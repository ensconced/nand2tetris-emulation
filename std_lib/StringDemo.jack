class StringDemo {
  static String str;

  function void runDemo() {
    let str = "Snow-Balls have flown their Arcs";

    while (str.length() > 0) {
      do Output.printString(str);
      do Output.println();
      do str.eraseLastChar();
    }
  }
}
